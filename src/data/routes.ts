export enum ERoute {
  Main = "/",
  Profile = "/profile",
  TrainingForm = "/training-form",
  GameForm = "/game-form",
  EventForm = "/event-form",
  TournamentForm = "/tournament-form",
  Shop = "/shop",
}
